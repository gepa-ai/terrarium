# Full AIME composition comparison without MetaHarness.
#
# Compares:
#   - GCA and AR in parallel, selecting best in-search result
#   - GCA -> AR sequential composition
#   - AR -> GCA sequential composition
#
# Composition budgets are total budgets. optimize_anything partitions them across the two
# inner stages/engines, so a two-engine composition with max_evals=800 gives
# each engine/stage a 400-call partition.
name: aime_gca_ar_compositions
source_zshrc: true
max_parallel_runs: 5

tasks:
  - name: aime_math
    task: aime_math
    overrides:
      - task.solver_lm=anthropic/claude-haiku-4-5
      - task.solver_temperature=0.0
      - task.solver_max_tokens=32000

seeds: [0]

budget:
  max_evals: 800
  max_token_cost: 20.0

defaults:
  - max_concurrency=32

algorithms:
  - name: parallel_gca_ar_best_of
    algorithm: optimize_anything
    overrides:
      - adapter.strategy=best_of
      - "adapter.configs=[{engine: gepa, split_train_val: true, engine_config: {reflection: {reflection_lm: null}, claude_code_agent: {model: sonnet}, engine: {parallel: true, max_workers: 32, seed: 0}}}, {engine: autoresearch, split_train_val: false, engine_config: {model: sonnet, ralph: true}}]"
      - benchmark.split_train_val=true

  - name: sequential_gca_then_ar
    algorithm: optimize_anything
    overrides:
      - adapter.strategy=sequential
      - "adapter.configs=[{engine: gepa, split_train_val: true, engine_config: {reflection: {reflection_lm: null}, claude_code_agent: {model: sonnet}, engine: {parallel: true, max_workers: 32, seed: 0}}}, {engine: autoresearch, split_train_val: false, engine_config: {model: sonnet, ralph: true}}]"
      - benchmark.split_train_val=true

  - name: sequential_ar_then_gca
    algorithm: optimize_anything
    overrides:
      - adapter.strategy=sequential
      - "adapter.configs=[{engine: autoresearch, split_train_val: false, engine_config: {model: sonnet, ralph: true}}, {engine: gepa, split_train_val: true, engine_config: {reflection: {reflection_lm: null}, claude_code_agent: {model: sonnet}, engine: {parallel: true, max_workers: 32, seed: 0}}}]"
      - benchmark.split_train_val=true
