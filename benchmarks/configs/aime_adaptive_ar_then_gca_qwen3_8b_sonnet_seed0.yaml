# One-run AIME adaptive sequential smoke/diagnostic.
#
# Purpose: test the adaptive_sequential scheduler with AutoResearch first,
# then GEPA + Claude Code Agent if AR plateaus.
name: aime_adaptive_ar_then_gca_qwen3_8b_sonnet_seed0
source_zshrc: true
max_parallel_runs: 1

tasks:
  - name: aime_math
    task: aime_math
    overrides:
      - task.solver_lm=openai/arbor:qwen/qwen3-8b
      - task.solver_temperature=0.0
      - task.solver_max_tokens=32000

seeds: [0]

budget:
  max_evals: 800
  max_token_cost: 10.0

defaults:
  - max_concurrency=32

algorithms:
  - name: adaptive_ar_then_gca
    algorithm: optimize_anything
    overrides:
      - adapter.strategy=adaptive_sequential
      - benchmark.split_train_val=true
      - adapter.scheduler.plateau_evals=90
      - adapter.scheduler.patience=1
      - adapter.scheduler.min_evals_per_stage=90
      - adapter.scheduler.improvement_epsilon=0.0
      - adapter.scheduler.cycle=false
    seed_overrides:
      - "adapter.configs=[{{engine: autoresearch, split_train_val: false, engine_config: {{model: sonnet, ralph: true, max_no_eval_seconds: 240}}}}, {{engine: gepa, split_train_val: true, engine_config: {{reflection: {{reflection_lm: null}}, claude_code_agent: {{model: sonnet}}, engine: {{parallel: true, max_workers: 32, seed: {seed}}}}}}}]"
