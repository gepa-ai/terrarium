# AIME comparison where optimizer spend is the primary budget.
#
# This intentionally removes the metric-call cap. A separate live monitor should
# enforce the experiment-level panic spend cap so solver spend cannot run away.
name: aime_optimizer_cost_cap_compare
source_zshrc: true
max_parallel_runs: 4

tasks:
  - name: aime_math
    task: aime_math
    overrides:
      - task.solver_lm=anthropic/claude-haiku-4-5
      - task.solver_temperature=0.0
      - task.solver_max_tokens=32000

seeds: [0]

budget:
  max_evals: null
  max_token_cost: 20.0

defaults:
  - max_concurrency=32

algorithms:
  - name: autoresearch
    algorithm: omni
    overrides:
      - adapter.backend=claude_code
      - ++adapter.config.model=sonnet
      - ++adapter.config.ralph=true
      - benchmark.split_train_val=false

  - name: gepa
    algorithm: omni
    overrides:
      - adapter.backend=gepa
      - ++adapter.config.reflection.reflection_lm=anthropic/claude-sonnet-4-6
      - ++adapter.config.engine.parallel=true
      - ++adapter.config.engine.max_workers=32
      - benchmark.split_train_val=true
    seed_overrides:
      - ++adapter.config.engine.seed={seed}

  - name: gepa_cc_agent
    algorithm: omni
    overrides:
      - adapter.backend=gepa
      - ++adapter.config.reflection.reflection_lm=null
      - ++adapter.config.claude_code_agent.model=sonnet
      - ++adapter.config.engine.parallel=true
      - ++adapter.config.engine.max_workers=32
      - benchmark.split_train_val=true
    seed_overrides:
      - ++adapter.config.engine.seed={seed}

  - name: sequential_ar_then_gca
    algorithm: omni
    overrides:
      - adapter.strategy=sequential
      - "adapter.configs=[{backend: claude_code, split_train_val: false, config: {model: sonnet, ralph: true}}, {backend: gepa, split_train_val: true, config: {reflection: {reflection_lm: null}, claude_code_agent: {model: sonnet}, engine: {parallel: true, max_workers: 32, seed: 0}}}]"
      - benchmark.split_train_val=true

  - name: sequential_gca_then_ar
    algorithm: omni
    overrides:
      - adapter.strategy=sequential
      - "adapter.configs=[{backend: gepa, split_train_val: true, config: {reflection: {reflection_lm: null}, claude_code_agent: {model: sonnet}, engine: {parallel: true, max_workers: 32, seed: 0}}}, {backend: claude_code, split_train_val: false, config: {model: sonnet, ralph: true}}]"
      - benchmark.split_train_val=true
