# Full AIME metric-budget replication with Qwen3-8B as solver.
#
# Metric calls are the primary budget for generalization comparisons.
# Optimizer token budget remains a guardrail.
name: aime_metric_budget_qwen3_8b_seeds_1_2
source_zshrc: true
max_parallel_runs: 5

tasks:
  - name: aime_math
    task: aime_math
    overrides:
      - task.solver_lm=openai/arbor:qwen/qwen3-8b
      - task.solver_temperature=0.0
      - task.solver_max_tokens=32000

seeds: [1, 2]

budget:
  max_evals: 800
  max_token_cost: 20.0

defaults:
  - max_concurrency=32

algorithms:
  - name: gepa
    algorithm: optimize_anything
    overrides:
      - adapter.engine=gepa
      - ++adapter.engine_config.reflection.reflection_lm=anthropic/claude-sonnet-4-6
      - ++adapter.engine_config.engine.parallel=true
      - ++adapter.engine_config.engine.max_workers=32
      - benchmark.split_train_val=true
    seed_overrides:
      - ++adapter.engine_config.engine.seed={seed}

  - name: gepa_cc_agent
    algorithm: optimize_anything
    overrides:
      - adapter.engine=gepa
      - ++adapter.engine_config.reflection.reflection_lm=null
      - ++adapter.engine_config.claude_code_agent.model=sonnet
      - ++adapter.engine_config.engine.parallel=true
      - ++adapter.engine_config.engine.max_workers=32
      - benchmark.split_train_val=true
    seed_overrides:
      - ++adapter.engine_config.engine.seed={seed}

  - name: autoresearch
    algorithm: optimize_anything
    overrides:
      - adapter.engine=autoresearch
      - ++adapter.engine_config.model=sonnet
      - ++adapter.engine_config.ralph=true
      - benchmark.split_train_val=false

  - name: sequential_gca_then_ar
    algorithm: optimize_anything
    overrides:
      - adapter.strategy=sequential
      - benchmark.split_train_val=true
    seed_overrides:
      - "adapter.configs=[{{engine: gepa, split_train_val: true, engine_config: {{reflection: {{reflection_lm: null}}, claude_code_agent: {{model: sonnet}}, engine: {{parallel: true, max_workers: 32, seed: {seed}}}}}}}, {{engine: autoresearch, split_train_val: false, engine_config: {{model: sonnet, ralph: true}}}}]"

  - name: sequential_ar_then_gca
    algorithm: optimize_anything
    overrides:
      - adapter.strategy=sequential
      - benchmark.split_train_val=true
    seed_overrides:
      - "adapter.configs=[{{engine: autoresearch, split_train_val: false, engine_config: {{model: sonnet, ralph: true}}}}, {{engine: gepa, split_train_val: true, engine_config: {{reflection: {{reflection_lm: null}}, claude_code_agent: {{model: sonnet}}, engine: {{parallel: true, max_workers: 32, seed: {seed}}}}}}}]"
