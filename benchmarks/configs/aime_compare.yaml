# Full AIME comparison template. Tune budgets before paper runs.
name: aime_compare
source_zshrc: true
max_workers: 1

tasks:
  - aime_math

seeds: [0]

budget:
  max_evals: 800
  max_token_cost: 3.0

defaults:
  - benchmark.split_train_val=true
  - max_concurrency=8

algorithms:
  - name: gepa
    algorithm: optimize_anything
    overrides:
      - adapter.engine=gepa
      - ++adapter.engine_config.reflection.reflection_lm=anthropic/claude-sonnet-4-6
    seed_overrides:
      - ++adapter.engine_config.engine.seed={seed}

  - name: gepa_cc_agent
    algorithm: optimize_anything
    overrides:
      - adapter.engine=gepa
      - ++adapter.engine_config.reflection.reflection_lm=null
      - ++adapter.engine_config.claude_code_agent.model=sonnet
    seed_overrides:
      - ++adapter.engine_config.engine.seed={seed}

  - name: autoresearch
    algorithm: optimize_anything
    overrides:
      - adapter.engine=autoresearch
      - ++adapter.engine_config.model=sonnet
      - benchmark.split_train_val=false

  - name: meta_harness
    algorithm: optimize_anything
    overrides:
      - adapter.engine=meta_harness
      - ++adapter.engine_config.model=sonnet
      - ++adapter.engine_config.max_iterations=null
      - benchmark.split_train_val=false
